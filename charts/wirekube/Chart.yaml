apiVersion: v2
name: wirekube
description: WireGuard mesh between Kubernetes nodes using CRDs for coordination. No VPN server, no external control plane.
type: application
version: 0.1.0
# Must track the latest release tag (git tag / GitHub release). The Makefile
# VERSION default is a local dev placeholder; do not copy it here.
appVersion: "v0.0.16"
kubeVersion: ">=1.24.0-0"
home: https://inerplat.github.io/wirekube/
sources:
  - https://github.com/inerplat/wirekube
keywords:
  - wireguard
  - mesh
  - vpn
  - networking
  - nat-traversal
maintainers:
  - name: inerplat
    url: https://github.com/inerplat
