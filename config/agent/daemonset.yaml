---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: wirekube-agent
  namespace: wirekube-system
  labels:
    app: wirekube-agent
    app.kubernetes.io/name: wirekube-agent
    app.kubernetes.io/component: agent
spec:
  selector:
    matchLabels:
      app: wirekube-agent
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: wirekube-agent
        app.kubernetes.io/name: wirekube-agent
        app.kubernetes.io/component: agent
    spec:
      serviceAccountName: wirekube-agent
      hostNetwork: true
      # Default (node resolver): no agent dial path needs cluster DNS (relay
      # endpoint comes from mesh spec/status, apiserver from the injected env
      # or the in-cluster IP literal), and cluster DNS is unreachable on
      # bootstrap nodes without a CNI.
      dnsPolicy: Default
      priorityClassName: system-node-critical
      tolerations:
        - operator: Exists
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: wirekube.io/proxy-node
                    operator: NotIn
                    values:
                      - "true"
                  # Node-scoped agent variants replace this DaemonSet on the
                  # nodes they claim; they must not run alongside it. Two agents
                  # on one node share /var/lib/wirekube and the interface name,
                  # so they present the same public key to the relay, which
                  # evicts each session as the other registers.
                  - key: wirekube.io/agent-canary
                    operator: NotIn
                    values:
                      - "true"
                  - key: kubernetes.io/os
                    operator: In
                    values:
                      - linux
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - wirekube-agent-proxy
                      - wirekube-agent-wss-canary
              topologyKey: kubernetes.io/hostname
      terminationGracePeriodSeconds: 30
      containers:
        - name: agent
          image: inerplat/wirekube:v0.0.18
          command: ["wirekube-agent"]
          args:
            - --node-name=$(NODE_NAME)
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: WIREKUBE_INTERFACE
              value: "wire_kube"
            - name: WIREKUBE_RELAY_TOKEN_FILE
              value: /var/run/secrets/wirekube-relay/token
            # Do not put HTTP_PROXY/HTTPS_PROXY here for clusters where only a
            # subset of nodes needs egress proxying. Label those nodes with
            # wirekube.io/proxy-node=true and deploy the dedicated proxy-node
            # example manifest instead.
            #
            # Bootstrap-only Kubernetes API override for nodes that cannot reach
            # the in-cluster Service IP before WireKube routes exist:
            # - name: WIREKUBE_KUBE_APISERVER
            #   value: "https://YOUR_KUBE_APISERVER_ENDPOINT"
            #
            # One-shot dataplane rebuild: the interface, routes, and rules
            # persist across restarts; set this to tear them down once at the
            # next start, then remove it.
            # - name: WIREKUBE_CLEAN_STATE
            #   value: "true"
          securityContext:
            # privileged is required to open /dev/net/tun on Ubuntu 24.04+
            # hosts where containerd's default device cgroup denies TUN
            # write even with CAP_NET_ADMIN. Older distros with permissive
            # device cgroups could get by with capabilities alone, but the
            # userspace engine needs the TUN device on every node so we
            # enable privileged here.
            privileged: true
            # Unconfined AppArmor: the default containerd profile denies
            # TUN-related operations on Ubuntu 24.04.
            appArmorProfile:
              type: Unconfined
            capabilities:
              add: ["NET_ADMIN", "SYS_MODULE"]
          volumeMounts:
            - name: wireguard-keys
              mountPath: /var/lib/wirekube
            - name: host-proc-sys-net
              mountPath: /host/proc/sys/net
            - name: dev-net-tun
              mountPath: /dev/net/tun
            - name: relay-token
              mountPath: /var/run/secrets/wirekube-relay
              readOnly: true
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 10m
              memory: 64Mi
      volumes:
        - name: wireguard-keys
          hostPath:
            path: /var/lib/wirekube
            type: DirectoryOrCreate
        - name: host-proc-sys-net
          hostPath:
            path: /proc/sys/net
            type: Directory
        - name: dev-net-tun
          hostPath:
            path: /dev/net/tun
            type: CharDevice
        - name: relay-token
          projected:
            sources:
              - serviceAccountToken:
                  path: token
                  audience: wirekube-relay
                  expirationSeconds: 3600
